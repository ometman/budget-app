databases:
  - name: budgetdb
    databaseName: budget_pro
    user: ometman
    plan: free

services:
  - type: web
    name: budget-app
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: postgres://ometman:fo0XEqZMRqLLV0Aq3zC8OJioB8G5ZdMA@dpg-cnada7gl5elc7396gc4g-a/budget_pro_xuxq
        fromDatabase:
          name: budgetdb
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default